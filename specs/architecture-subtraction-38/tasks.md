# Tasks

- [x] 冻结 `CliEngine` 调用图、transport/persistence 边界与基线。
- [x] 先同步 Round38 规格、ACP active contract、Store wiki 与长期减法决策。
- [ ] 删除 `CliEngine` 别名、imports 与二次重导出，统一使用 `RuntimeCliEngine`。
- [ ] 补强架构守卫并验证 daemon→Store→TaskDetail runtime detection 链。
- [ ] 完成 TypeScript、定向测试、构建、全量测试与独立复审。
- [ ] 归档规格并完成交付门禁。
