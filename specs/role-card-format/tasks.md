# Role Card Format Tasks

- [ ] 对照当前 TeamPack、RoleCard repository 与导入 API 核实字段名称和必填规则。
- [ ] 明确 `role-card/0.1` 与 `team-pack/0.1` 的兼容、升级和拒绝策略。
- [ ] 为 `role.json`、`pack.json`、SOUL.md 路径和 communication matrix 建立 schema 验证测试。
- [ ] 验证 `examples/example-team-pack/` 可以通过真实导入流程。
- [ ] 删除 spec 中仅属于任务模板、但不属于格式协议的段落或字段。
- [ ] 将稳定格式契约回写到 `docs/technical/integrations/` 后归档本规格。

