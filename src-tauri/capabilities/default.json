{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Renderer receives window access only; business and OS capabilities stay in the Host/Service.",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:default"]
}
