# 协作效率复盘 v2 · 定稿 (2026-07-14)

> 统筹：@mario｜中心辐射
> 合并来源：v1（本文件前身）+ `2026-07-14-collaboration-retrospective.md`（早期草稿，已被本稿取代）+ `collab-feedback-peach-2026-07-14.md` + dk/luigi 群聊反馈
> 触发：用户反馈「token 烧得多、事情没推进」+ 本轮追加洞察「**任务传递不好，缺一个上下文管理器**」
> 证据基线：`.ath/TASKS.md`（14 任务 / 0 done / 1 doing / 13 待）+ R3–R11 风险表 + 实跑记录
> 状态：**v2 定稿**，提交 git 并 push。

---

## 〇、一句话诊断（v2 升级版）

> 不是没干活，是 **干的活在 agent 之间蒸发**。我们正给用户 agent 造一个 `ContextManager`，而团队协作本身缺一个同构的上下文管理器——这才是比「Gate 标准不一致」更深的元病灶。

排兵布阵地说：我们养了一支四人的精锐（DK 把关、Luigi 执行、Peach 验收、Mario 统筹），但**每次调兵遣将，命令到了前线就只剩半截**——前线要自己重新侦察（重读代码、重跑测试、重收集上下文）。token 不是烧在打仗上，是烧在「每个士兵每次都要重新画一遍地图」上。

---

## 一、核心论点：协作层缺一个上下文管理器 ⭐⭐⭐⭐（用户洞察，本轮最高优先级）

### 1.1「上下文管理器」的双重含义

| 层 | 含义 | 现状 |
|----|------|------|
| 产品层（在造的） | `ContextManager.ts` / `PromptComposer`：把预算、分层、记忆、身份组装成一个**不破的系统提示**，喂给单个 agent | TASK-002/003 已三轮通过，进入 review |
| 协作层（缺的） | 跨 agent 的**任务交接上下文**：A2A handoff 时，把任务上下文、决策待办、自检证据、diff 范围打包成一个结构化快照传给下游 | **空白**——目前靠自然语言散文手工搬运 |

两者同构：前者解决「同一个 agent 的上下文不丢」，后者解决「上下文在 agent 之间不丢」。我们正专心造前者，却忘了后者。

### 1.2 症状映射：六轮拉锯的每一次返工，都是上下文蒸发的实证

- **每轮评审从零重建**（Peach 视角 G5）：六轮 review，每轮开场都「先读看板 + 重读核心改动」。没有可复用的「评审上下文快照」→ 六轮 = 六倍 token。
- **reject 备注写成 600 字散文**（R11 风险行）：人在用自然语言手工搬运上下文——这本该是结构化数据（必修项 / 行号 / 验收命令）。下游 Luigi 收到散文，漏修 B5（修了 B1 漏 B5）。
- **「知会 @dk」不触发执行**：上下文传过去了，但「待决策项」没有结构化载体，决策悬空整晚（R5/R7）。
- **交付自报与实跑不符**（R11 PARSE_ERROR）：Luigi 声称「73/73 全绿」，实跑收 0 用例——「自检证据」这一段上下文是空的，Peach 只能自己重跑。

### 1.3 与正在开发的技术产品形成自指闭环

这个论点妙在**自指**——我们的病症正是我们的药方：

- 正在做的 `ContextManager` 解决「单 agent 上下文不丢」
- 正在做的 **TASK-010（跨项目身份契约 IdentitySnapshot / ScopedContext）** 已经在构思「跨边界上下文契约」
- 缺的只是把它从「跨项目」延伸到「跨 agent 任务交接」——给 A2A handoff 也装一个上下文管理器

**最高杠杆解**：把 **TASK-007（`renderDispatchPrompt` 退役 → `a2aHandoff` source）** 升级为携带**结构化交接快照**（任务上下文 + 决策待办 + 自检证据 + diff 范围），让接收方不必从零重建。这一刀下去，六轮拉锯里的「重收集」直接归零。

---

## 二、数据真相（不是感觉，是账）

| 信号 | 证据 | 含义 |
|------|------|------|
| P1 全链卡 review | TASK-001~006 全在 `review`/`doing`，**无一 `done`** | 没有一刀真正落袋 |
| TASK-006 六轮打回 | R8（三轮）→ R10（五轮）→ R11（六轮，00:29） | 同一收口任务反复拉锯 |
| TASK-004 状态横跳 | `in_review → blocked → in_review` | 状态机空转，代码没动几行 |
| 交付与实跑不符 | R11：报「73/73 全绿」，实际 PARSE_ERROR 收 0 用例 | 评审方已不信任自报 |
| 工具链盲区 | `tsconfig.json:33 exclude:["**/*.test.ts"]` | tsc 根本不验证测试，却长期当「通过证据」 |
| 决策悬置 | R4/R5/R7 三处挂「待 @dk 拍板」 | 架构 gate 空转，每轮重议同一决策 |

**结论**：不是没干活，是上下文在 gate 前反复蒸发。病灶在「什么算通过」「决策谁拍」**以及最深的——「上下文怎么传」**这三条链上。

---

## 三、根因（置于核心论点之下，按杀伤力排序）

| # | 根因 | 杀伤力 | 归口 |
|---|------|--------|------|
| 0 | **跨 agent 上下文蒸发**（见第一章） | ⭐⭐⭐⭐ | TASK-007 升级（M8） |
| 1 | Gate 标准双向不一致：Luigi 用 `tsc EXIT 0`，Peach 用 `vitest` 全量；tsconfig exclude test → tsc 对测试零证明 | ⭐⭐⭐ | D1 / M1-M2 |
| 2 | 交付自报与实跑不符 → 信任坍塌 → Peach 退化成全量重测 | ⭐⭐⭐ | D1 / M2 |
| 3 | 架构决策空转：R4/R5/R7 长期挂「待 @dk」，无 SLA、知会≠推进 | ⭐⭐ | D2 / M5 |
| 4 | 收口任务过载 + 串联太重：TASK-006 一点挂，整条 P1 卡，P2 全 todo | ⭐⭐ | D3 / M4 |
| 5 | 状态机噪音：横跳与六轮通知制造「很忙」错觉 | ⭐ | M5（Mario 收口） |

---

## 四、各角色视角集合（补全 v1「待补充」项）

> 来源：群聊反馈 + `collab-feedback-peach-2026-07-14.md` + TASKS.md 证据。

### 🏛️ @dk（架构 gate）——决策被「知会」而非「请求」
- **已落地**：TASK-004 C1 决策（id 空守卫）已拍板**推荐方案 A**（防御式 `if (project.id)` 守卫，与 name/path 对称）。
- **观察**：R5/R7 用「知会 @dk」等待，但**知会不会触发执行**（协议明确），决策迟迟不动 → gate 卡死。结构性盲区 `tsconfig:33` 排除 test 文件，应单独立项修掉，而非在评审里反复绕过。
- **建议**：架构决策走 A2A（`@dk 请 决策 + 具体选项`），设响应时限；评审发现的结构性陷阱转成实现任务，不每轮点名。

### 💻 @luigi（全栈实现）——职责边界模糊 + 自测缺位
- **观察**：任务分派存在职责错配（TASK-005 历史上挂 backend 实际由 luigi 承接）；反复只跑 `tsc` 不跑全量 `vitest`，`tsconfig` exclude 盲区此前未察觉。
- **病灶**：B1 修 await 方向正确却引入 PARSE_ERROR 回归（改完没跑全量就交）；B5/R7 跨多轮没收口，依赖 dk 决策但不主动升级催办。
- **建议**：交付前自测纪律——全量 `npx vitest run` + 实跑输出贴交付说明，否则不进 review。

### 🔍 @peach（quality_gate）——评审沦为替实现者调试
- **根因**（完整见 peach feedback）：G1 DoD 未前置到 implementing；G2 结构盲区不根除；G3 决策无 SLA；G4 reject 是散文式风险行；G5 评审每轮重收集上下文。
- **自我反思**：六轮打回，评审 side 也有责任——本应第二轮就要求附实跑输出（G1），而非自己重复跑六遍。
- **改进承诺**：评审前先核自检证据齐全，缺则拒收；打回必附「编号 checklist + 验收命令」；架构阻塞立即 A2A 升级。

### 🎯 @mario（统筹，自我反思）——收口缺位，放任拉锯
- **失职**：TASK-002（R3 已记三轮通过）、TASK-005（R9 已记修复）状态仍停在 review/doing——**已通过的没人标 done，看板失真**。
- **失职**：R5/R7 决策悬空整晚，我没主动 A2A 拉 dk 拍板。统筹本该「发现卡点就拉人」，我等到用户发火才补课。
- **失职**：TASK-006 挂六轮没及时拆分（D3 本该第三轮就做）；风险行只增不收敛，没有定期「合并/降级/关闭」。
- **本轮补课**：D1/D2/D3 + M1–M8，并把「上下文管理器」这一元病灶正式纳入看板。

---

## 五、即时决策（Mario 本轮拍板）

### D1：锁定 P1 Gate 定义
**P1 通过唯一判据 = `npx vitest run`（全量）全绿 + 实跑输出贴入交付说明。**
- `tsc --noEmit` EXIT 0 仅作类型补充证据，**不单独构成通过**（tsconfig:33 exclude 测试）。
- 交付未附 vitest 实跑输出 → 自动打回，不进入评审。

### D2：R7 立即拍板 → 方案 A（防御式守卫）✅ dk 已推荐 A
- `projectLayer.ts:3` 加 `if (project.id)` 守卫（与 name/path 对称）；`ContextManager.ts:231` 从 `req.conversationId` 取 id。
- R5/R4 维持 P2 债务，不阻塞 P1。

### D3：拆 TASK-006 → 三个独立 gate
| 子任务 | 内容 | Gate |
|--------|------|------|
| 6a | 删 `promptComposer.test.ts:540-547` 重复块，修 PARSE_ERROR → 套件用例 >0 全绿 | 独立 |
| 6b | 按 D2 落地 R7 | 独立 |
| 6c | 全量 `npx vitest run` P1 100% 绿 + build 通过 | 依赖 6a/6b |

拆后 6a/6b 并行，任一挂不再拖垮整条线。

### D4（新增）：本轮提交隔离
本次复盘提交**仅 `docs/daily/`**，隔离 `src/` 未过审代码（TASK-006 仍在 review）。代码走正常 quality_gate 流程，不借复盘提交绕过。

---

## 六、改进措施清单（M1–M8）

| # | 措施 | 解决根因 | 责任 | 状态 |
|---|------|---------|------|------|
| M1 | 锁定 Gate 定义（D1），写入 `.ath/PROTOCOLS.md` | 1 | Mario | ⏳ 待建 PROTOCOLS.md |
| M2 | 交付前自测：全量 vitest + 实跑输出，否则自动打回 | 1,2 | Luigi | 待执行 |
| M3 | R7 拍板 A（D2），落地 projectLayer/ContextManager | 3 | Mario→Luigi | ✅ dk 已推荐 |
| M4 | 拆 TASK-006 → 6a/6b/6c（D3） | 4 | Mario 拆 / Luigi 做 | ⏳ 待拆 |
| M5 | 架构决策 SLA：挂起超 24h，Mario 按 recommended 默认拍板；阻塞决策一律走 A2A | 3 | Mario / dk | 制度 |
| M6 | 通过单 checklist：Luigi 自检 + Peach 仅抽查边界，不再全量重测 | 2 | Peach 重建 | 待 Peach 定 |
| M7 | 决策债务清单独立成区（R4/R5），不混 P1 gate | 3 | Mario | 制度 |
| **M8** | **【最高杠杆·本轮新增】给 A2A handoff 装上下文管理器**：TASK-007 升级为携带结构化交接快照（任务上下文 + 决策待办 + 自检证据 + diff 范围），与 ContextManager / IdentitySnapshot(TASK-010) 闭环；reject 备注改为「编号 checklist + 验收命令」结构 | **0（元病灶）** | Mario 立项 / Luigi 实现 | ⏳ 待立项为 TASK-007 的验收项 |

---

## 七、最短解封路线

```
[1] @dk 拍板 R5（或维持 P2 债）          ← R7 已推荐 A，仅余 R5
        │
[2] @luigi 一次修干净 TASK-006（6a/6b/6c）← 删重复块 / 落地 R7 / 全量 vitest + 实跑输出
        │
[3] 改 tsconfig:33 或加 vitest typecheck ← 根治「假绿」（单独立小任务）
        │
[4] @mario 收口状态机                    ← TASK-002/005 已通过的标 done
        │
[5] TASK-006 → done                      ← 解锁 P2 全链（007~014）
        │
[6] TASK-007 按 M8 升级                  ← 给 A2A 装上下文管理器，根治元病灶
```

**第 1 步是钥匙**，第 6 步是断根。

---

## 八、预期效果与验证信号

**目标**：把「改 → 交 → 被打回 → 再改」的 6 轮，压成「自测通过 → 抽查通过 → done」的 1–2 轮；并让交接上下文随任务流动，不再每轮重建。

**下次复盘看这几条**：
- TASK-006 拆分后，6a/6b/6c 是否 ≤2 轮各自 done
- 是否再出现「声明全绿 / 实跑不符」
- A2A handoff 是否携带结构化快照（M8 落地）
- 架构决策是否还在 P1 gate 空转
- P2（TASK-007+）能否顺利启动

---

*本文件由 @mario 总成，合并 v1 + retrospectives.md（草稿）+ peach feedback + dk/luigi 群聊反馈。v2 定稿，提交 git 并 push 到 origin/main。*
