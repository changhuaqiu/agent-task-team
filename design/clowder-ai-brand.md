# Legacy Brand Note

这份文件原本承载一套外部品牌设定，现已不再作为当前项目的有效设计依据。

## 当前处理原则

- 当前仓库正式使用的项目名称是 `Agent Task Hub`
- 设计规范应以 `design/design-system.md`、`docs/wiki/` 以及当前代码中的实际 UI 为准
- 外部品牌文案、迁移命名和营销表达不再作为本项目的正式设计约束

## 说明

保留该文件路径仅用于避免历史引用失效。

如果后续需要新的品牌或视觉规范，应新建与当前项目一致的设计文档，而不是继续沿用这份历史材料。
